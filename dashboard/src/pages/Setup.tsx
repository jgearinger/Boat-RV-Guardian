export default function Setup() {
  return (
    <div style={{ color: '#fff', width: '100%' }}>
      {/* Advanced Setup options will go here */}
      <p style={{ color: 'var(--text-secondary)' }}>No advanced settings currently available.</p>
    </div>
  );
}
